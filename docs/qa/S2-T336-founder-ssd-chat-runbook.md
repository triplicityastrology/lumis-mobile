# S2-T336 Founder SSD Chat runbook

Use only branch `codex/s2-t336-chat-after-dice-ssd` in the persistent SSD worktree. Run `pnpm start:s2-t336-founder-chat-simulator` for localhost Simulator/Expo Go testing or `pnpm start:s2-t336-founder-chat-expo` for the LAN Expo session.

The launchers resolve the repository from their own location, reject another branch, reject tracked changes, reject occupied ports without killing a process, and clear Supabase public environment values. The visible screen is an offline fixture and cannot navigate from Dice, call a provider, load a member, save messages, charge units, or expose a public route.

Review EN and Traditional Chinese loading/reply/safety/fallback pixels on the unchanged polished Chat surface. Technical-error truth is covered by the focused emulator and contract: `assistant_message` is absent and localized presentation text says nothing was saved or charged. No live-device, provider, deployment, or normal-member evidence is claimed.
