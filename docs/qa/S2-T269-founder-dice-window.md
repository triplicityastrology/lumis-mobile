# S2-T269 Founder Dice window

## Authority

This development-only journey consumes the documented S2-T262 `dice_synthetic_gateway_port_v1` contract. It does not contain a provider client or a network call. Both accepted evidence constants remain `null`, so the current build can prepare questions and review offline previews but cannot claim or invoke a live synthetic result.

`NO_NORMAL_CHAT_INTEGRATION_AUTHORITY`

`NO_AZURE_TRAFFIC_AUTHORITY`

## Founder browser path

1. Run `pnpm start:s2-t269-founder-dice-web` from this worktree.
2. Open `http://localhost:8143`.
3. Confirm the full build SHA and `founder-dice-window` state in the evidence strip.
4. Review the Technical evidence gate. It truthfully remains waiting until a separately accepted 80-case receipt is compiled into the boundary.
5. Draft customer-style synthetic questions. Do not use names, birth data, member details, account details, or device details.
6. Freeze exactly 20 English and 20 Traditional Chinese slots, then prepare the fixture checksum.
7. The authorization request stays disabled until accepted Technical evidence exists.
8. Offline previews may be rated. A live synthetic label appears only after an accepted authorization and closed execution envelope.
9. A future window is complete only after the post-window disable proof verifies closed and disabled.

## Simulator path

Boot the configured iPhone Simulator with Expo Go, then run `pnpm start:s2-t269-founder-dice-simulator`. The launcher uses port 8144, clears Metro cache, prints the exact SHA, and never kills an existing process.

## Now versus later

Now: drafting, deterministic validation/classification, exact 20/20 freezing, offline preview, ratings, checksum exports, hostile evidence rejection, large-text layout, and exact-build launch.

Later: import the independently accepted 80-case Technical receipt, submit the Founder authorization request, invoke allow-listed fixture IDs, display checksum-verified live synthetic interpretations, and verify the post-window disabled receipt.

No history, persistence, units, member authentication, raw provider diagnostics, or provider call is available from this surface.

## Rollback

`git revert <S2-T269-commit>`
