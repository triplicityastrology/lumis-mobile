# S2-T180 Care Circle QR and push readiness

This source remains development-only and inactive. The current Expo bundle does not include `expo-camera`, so the Founder path truthfully presents manual four-digit entry. Camera testing starts only after the SDK-matched native module is installed, the real camera adapter is wired, and a native bundle check passes.

Device path: Founder Tests > Care Circle > local rehearsal > People you care for > Scan or enter someone's code. Confirm the unavailable-camera state, exact four-digit numeric entry, invalid payload rejection, pending/no-authority state, and manual fallback.

Lifecycle push is default-off. The durable participant-scoped inbox event is written first; denied permission, a missing token, or provider failure cannot discard it. No provider credentials, delivery activation, or remote invocation are included.
