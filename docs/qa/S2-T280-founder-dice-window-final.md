# S2-T280 final Founder Dice window

## Authority

This development-only journey consumes stable closed schemas rather than unfinished T277/T279 source. It first requires an independently accepted final runtime package rooted in completed T272, then an independently accepted 80-case Technical evidence envelope bound to the completed T274 PG17 receipt, and finally a separate Founder-window receipt. All acceptance digest allow-lists are `null` in this commit.

`NO_NORMAL_CHAT_INTEGRATION_AUTHORITY`

`NO_AZURE_TRAFFIC_AUTHORITY`

## Founder browser path

1. Run `pnpm start:s2-t280-founder-dice-web` from this worktree.
2. Open `http://localhost:8147` and confirm the full Git SHA plus state `founder-dice-window-final`.
3. Review the final runtime and Technical evidence gates. Both truthfully remain waiting until separately accepted receipt digests are compiled into the boundary.
4. Draft customer-style synthetic questions without names, birth data, member/account/device details, or real customer text.
5. Freeze exactly 20 English and 20 Traditional Chinese slots and prepare the fixture checksum.
6. Generate the checksum-only Founder authorization request. Question text is excluded.
7. A live synthetic result remains impossible until a separate Founder receipt and per-fixture execution evidence are independently accepted.
8. Rate verified output and export the checksum verdict package.
9. Finish only after the post-window proof confirms gateway disabled, provider unavailable, and zero post-close calls.

## Simulator path

Boot the iPhone 17 Simulator with Expo Go, then run `pnpm start:s2-t280-founder-dice-simulator`. The launcher uses dedicated port 8148, validates the exact branch and clean commit, clears Metro cache, prints the exact SHA, and never kills another process.

## Now versus later

Now: pre-login drafting, deterministic validation/classification, exact 20/20 freezing, offline previews, ratings, checksum exports, hostile receipt rejection, large-text layout, web export, Simulator/native bundle validation, and exact-build launch.

Later: import accepted runtime, 80-case Technical and Founder-window receipts; invoke allow-listed Founder fixture IDs only; display checksum-verified interpretations; and prove post-window disable. No live result is inferred from local data.

No history, persistence, units, member authentication, raw provider diagnostics, or provider call is available from this surface.

## Rollback

`git revert <S2-T280-commit>`
